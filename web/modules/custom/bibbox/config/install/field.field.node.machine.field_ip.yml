status: true
dependencies:
  config:
    - field.storage.node.field_ip
    - node.type.machine
id: node.machine.field_ip
field_name: field_ip
entity_type: node
bundle: machine
label: IP
description: "The IP of the machine. NB! Make sure this is Unique so machines are not mixed! Should start with https:// to use ssl connection."
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string
